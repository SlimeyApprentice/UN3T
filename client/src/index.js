import React from 'react';
import ReactDOM from 'react-dom/client';
import StrictMode from 'react';
import ZoomViewWrapper from './ZoomViewWrapper.jsx';
import './styles.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <ZoomViewWrapper />
);